--CREATE TABLE CUSTOMER (ID INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), "ID"), NAME VARCHAR(255), EMAIL VARCHAR(255), PASSWORD(255);

CREATE TABLE CUSTOMERS (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY,
    name VARCHAR(255),
    email VARCHAR(255) NOT NULL,
    password VARCHAR(100) NOT NULL,
    PRIMARY KEY (id)
);



